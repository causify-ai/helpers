Move the logic of the following wrappers 
rigc      rigdef    rigrule   rigtodo   rigtodoc

inside rig

E.g., rigdef -> rig --def, rigrule -> rig --rule, rigtodo -> rig --todo

Add a --cfile to save the output of in a cfile and call vic on it

- If the task is not perfectly clear, you MUST not perform it, but ask for
  clarifications

- When writing code you must always follow the instructions in
  `@.claude/skills/coding.rules.md`
