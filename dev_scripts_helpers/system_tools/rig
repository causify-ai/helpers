#!/usr/bin/env python3
"""
rig - Utility for ripgrep searches and directory operations.
Python wrapper around ripgrep (rg).
"""

import sys

import helpers.hrig as hrig

if __name__ == "__main__":
    exit_code = hrig.main(sys.argv[1:])
    sys.exit(exit_code)
