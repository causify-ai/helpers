#!/bin/bash -e

# Use a number suffix if provided (e.g., `instr 2` creates instr2.md).
SUFFIX="${1:-}"
INSTR_FILE="instr${SUFFIX}.md"

vimdiff ./helpers_root/docs/ai_templates/ai.instruction_template.md "$INSTR_FILE"
