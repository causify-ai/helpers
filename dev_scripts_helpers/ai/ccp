#!/bin/bash -xe
# """
# Run Claude Code CLI in non-interactive (print) mode with a prompt.
# - Runs with --dangerously-skip-permissions flag.
# - Outputs in plain text format.
# - Usage: ccp <prompt>
# """

claude --dangerously-skip-permissions --output-format text -p "$*"
