#!/bin/bash -e
# """
# Print the files in the client that are potentially useful
# """

gs | \grep -v ?

echo
echo "========== Potentially useful ==========="
gs | \grep ? | \grep -vE "(^| |/)tmp\." | \grep -v "/outcomes/" | \grep -E "\.(py|md|txt)$"
