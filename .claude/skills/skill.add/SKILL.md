---
description: Add a rule to the set of rules
---

- Given the request of the user decide in which of the files `<file>`
  ```bash
  > ls -1 .claude/skills/*.rules.md
  ```

  to add the proposed rule

- Then add the rule to the file `<file>` following the conventions in
  `.claude/skills/skill.rules.md`
